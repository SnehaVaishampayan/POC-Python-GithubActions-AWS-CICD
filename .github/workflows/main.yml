name: Infrastructure deployment

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AWS_DEFAULT_REGION: us-west-1

    steps:
    - name: checkout
      uses: actions/checkout@v2

    - name: Extract branch name
      shell: bash
      run: echo "##[set-output name=branch;]$(echo ${GITHUB_REF#refs/heads/})"
      id: extract_branch

    - name: Get lambda function name
      id: lambda-function-name
      run: |
          if  ${{ steps.extract_branch.outputs.branch == 'main' }}; then
            echo "::set-output name=function-name:: 'gsp-create-project'"
          else
            echo "::set-output name=function-name:: gsp-create-project-${{steps.extract_branch.outputs.branch}}"
          fi
            echo "finish"

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: AKIA4BK26LWH5YPUBS5S
        aws-secret-access-key: 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
        aws-region: ${{ env.AWS_DEFAULT_REGION }}

    - name: fe2
      id: ex    
      shell: bash
      run: echo "##[set-output name=bh;]$(aws lambda get-function --function-name ${{steps.lambda-function-name.outputs.function-name}})"
    
    - name: print details
      run: |
          echo "print branch ${{ steps.ex.outputs.bh }} "
            
    
        
          

